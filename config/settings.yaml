timezone: America/New_York

symbols:
  primary: /NQ
  smt_compare: /ES

session:
  cash_open: "09:30"
  orb_start: "09:30"
  orb_end: "09:45"
  eod_exit: "16:00"

strategy:
  orb:
    min_range_points: 5.0
  liquidity:
    swing_lookback: 20
    sweep_buffer_points: 0.50
    sweep_confirm_close_back_inside: true
  fvg:
    entry_timeframes_min: [1, 3, 5]
    higher_tf_min: 240
    min_gap_points: 0.25
    max_fvg_age_min: 120
    ifvg_reaction_candles: 2
  risk:
    stop_buffer_points: 0.50
    default_rr: 2.0

campfire:
  url: "https://community.atradeortwo.com"
  room_id: "15"  # ORB + Gooey Model alerts (separate from standard ORB in room 5)

logging:
  level: INFO
  file: logs/orb-gooey-trader.log
